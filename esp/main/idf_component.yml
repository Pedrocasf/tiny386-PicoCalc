## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: '>=4.1.0'
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  espressif/esp_lcd_axs15231b:
    matches:
    - if: idf_version < 6.0
      version: ==1.0.0
    - if: idf_version >= 6.0
      version: ==2.1.0
  espressif/esp_lcd_st7701:
    version: ==2.0.2
    rules:
    - if: idf_version >= 5.5
  espressif/esp_lcd_touch_gt911: ^1
  espressif/esp_wifi_remote:
    version: '*'
    rules:
    - if: "target in [esp32p4]"
  espressif/esp_hosted:
    version: ==2.12.3
    rules:
    - if: "target in [esp32p4]"
  espressif/esp_codec_dev:
    version: ^1.3.4
    rules:
    - if: "target in [esp32p4]"
  usb_host_hid: "^1.0.1"
  # esp/main/usb_msc.c (USB mass-storage device mode) additionally needs
  #   espressif/esp_tinyusb: {version: "^2.2.1", rules: [{if: "target in [esp32p4]"}]}
  # It is left out because no board here enables USE_USB_MSC, and the component
  # is linked in whole even when nothing references it -- about 60 KB.
